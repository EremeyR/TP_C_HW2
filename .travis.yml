dist:     focal
sudo:     required
language: c
os: linux

install:
    - sudo apt-get update
    - sudo apt-get --yes libgrest-dev
    - sudo apt-get --yes gcovr

jobs:
    - stage: Build
      script:
          - cmake -B build
          - make -C build

